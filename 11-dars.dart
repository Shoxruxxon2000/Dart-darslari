void main(List<String> args) {
  List<int> list = [1, 2, 3, 4, 5, 6, 7, 8, 9];
  List<int> list1 = [9,8,7,6,5,4];
 
  // list.add(1); // Listga element qo'shadi.
  // print(list);

  // list.addAll(list1); // To'liq listni qo'shadi.
  // print(list);

  // var t = list.length; //listni uzunligini aniqlaydi.
  // print(t);

  // var t = list.reversed; //Listni teskari ko'rinishga o'tkazadi.
  // print(t);

  // list.clear(); // listni tozalab beradi.
  // print(list);

  // var t = list.runtimeType; // qanday turdaligini aniqlab beradi.
  // print(t);

  // var t = list.first; // Listning birinchi elementini chiqarib beradi.
  // print(t);
  // print(list[0]);

  // var t = list.last; // Listning oxirgi elementini topib beradi.
  // print(t);
  // print(list[8]);

  // var t = list.isEmpty; // Listni  bo'shligini tekshiradi.
  // print(t);

  // var t = list.isNotEmpty; // Listni  bo'shligini tekshiradi.
  // print(t);

  // var t = list.single; // Listni bitta elementi bo'lsa chiqaradi.
  // print(t);

  // var t = list.contains(20); // Listni  ichidagi elementlarini solishtiradi.
  // print(t);

  // var t = list.elementAt(2); // Listni  indexini beramiz elementini chiqarib beradi.
  // print(t);

  // var t = list.getRange(1, 5); // Listni  elementlarini berilgan oraliqda olib beradi.
  // print(t);

  // var t = list.indexOf(3); // Listni elementi beriladi indexini chiqarib beradi.
  // print(t);

  // list.insert(3, 20); // Listda indexi bo'yicha elementini joylashtirib  beradi.
  // print(list);

  // list.insertAll(3, [1,2,3]); // Listdagi berilgan index
  // print(list);               // bo'yicha elementlarni joylab beradi.

  // var t = list.join(); // Listdagi elementlarni hammasini bitta qilib chiqarib beradi.
  // print(t);

  // var s = 0; // Hamma elementlar yig'indisi chiqarib beradi.
  // var t = list.reduce((value, element) => value += element);
  // print(t);

  // var t = list.remove(20); // Listni elementini o'chirib beradi.
  // print(t); //o'chirganini rostlikka tekshiradi.
  // print(list);

  // var t = list.removeAt(5); // Listni indexi bo'yicha elementini o'chiradi.
  // print(t);  // Listdagi o'chirilgan element
  // print(list);

  // var t = list.removeLast(); // Listni  oxirgi elementini o'chiradi.
  // print(t); // Listdagi o'chirilgan element
  // print(list);

  // list.removeRange(2, 5);
  // print(list);// Listdagi  berilgan indexlar oralig'idagi elementini o'chiradi.

  // list.setAll(1, list1); // Listdagi berilgan indexdan boshlab
  // print(list); //o'zining elementlarini o'rniga joylashtirib beradi.

  // list.setRange(3, 6, [1,2,3,4]); //Listdagi elementlarni faqat 
  // print(list); //  berilgan oralig'ini almashtilib beradi.

  // list.shuffle(); // list elementlarini random orqali aralashtirib beradi.
  // print(list);

  // var t = list.singleWhere((element) => element > 8);
  // print(t); // Listdagi hamma elementni ko'rib chiqib shart bo'yicha bitta element qaytaradi.

  // var t = list.skip(5); // Listdagi elementlarni berilgan 
  // print(t); //qiymat bo'yicha boshidan qirqib tashlaydi.

  // list.sort(); //Listdagi elementlarni saralab beradi.
  // print(list);

  // var t = list.sublist(3); // Listdagi elementlarini berilgan qiymat bo'yicha o'chirib tashlaydi 
  // print(t);

  // var t = list.take(3); // Listdagi elementlarini berilgan qiymat bo'yicha olib beradi.
  // print(t);

  // var t = list.toString(); // Listni String turiga o'tkazadi.
  // print(t);
}
